services:
  theta-terminal:
    build:
      context: .
      dockerfile: Dockerfile
    image: theta-terminal:latest
    environment:
      - THETADATAUSERNAME
      - THETADATAPASSWORD
      - THETATERMINALID
