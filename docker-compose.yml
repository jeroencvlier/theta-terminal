version: '3.8'
services:
  theta-terminal:
    build:
      context: .
      dockerfile: Dockerfile
    image: theta-terminal:latest
    env_file:
      - .env
    ports:
      - "25500:25500"
    restart: unless-stopped
